{#
Render a list of country tags.

Optional:

title - Title for the block.
#}

{% with sectors=page.sectors.all() %}
    {% if sectors|length > 0 %}
        <p>
            <strong>{% if title %}{{ title }}{% else %}{% trans %}Sectors{% endtrans %}{% endif %}</strong><br>
            {% for sector in sectors %}
                <a href="{{ url('sector-tag', kwargs={'section_slug': page.section_page.slug, 'tag_slug': sector.slug}) }}">{{ sector.name }}</a>{% if not loop.last %}, {% endif %}
            {% endfor %}
        </p>
    {% endif %}
{% endwith %}
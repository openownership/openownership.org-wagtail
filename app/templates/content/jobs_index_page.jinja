{% extends 'base.jinja' %}


{% block content %}
<div class="contained-width job-index-page">

    <div class="aside-layout-left">
        <div class="aside-layout__menu">
            {% block menu %}
                {% with title="Opportunities" %}
                    {% include "_partials/page_menu.jinja" %}
                {% endwith %}
            {% endblock menu %}
        </div>

        <div class="aside-layout__main">
            <h1 class="heading-xl">{{ page.title }}</h1>

            <div class="intro">
                {{ page.intro|richtext }}
            </div>
        </div>
    </div>
</div>

{% endblock content %}


